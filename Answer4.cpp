#include <iostream>

#include <cmath>

using namespace std;

int main()
{
	float a, b, c;
	int i = -1;
	cout << "Find roots of quadratic equation ax2  + bx + c=0 ";
	if (a == 0 || b == 0 || c == 0)
	{
	cout << "The value cannot be zero" << endl;
	}
	else if(a < 0 || b < 0 || c < 0)
	{
	cout << "Values must be greater than Zero" << endl;
	}
else {
		disc = (b * b) - 4 * a * c;

return 0;